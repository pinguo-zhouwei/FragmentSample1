<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.camera360.fragment">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.camera360.fragment.HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".CommunicationActivity" />
        <activity android:name="com.camera360.fragment.day2.SimpleActivity" />
        <activity android:name="com.camera360.fragment.day2.DetailsActivity" />
    </application>

</manifest>
